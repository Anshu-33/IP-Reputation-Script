# IP Abuse Checker

A lightweight script that checks IP addresses against AbuseIPDB.

## Setup
1. Install requirements:
   pip install requests pandas

2. Add your API key in the script:
   API_KEY = "your_key"

3. Put IPs in a CSV (one per line) and set CSV_PATH.

4. Run:
   python ip_check.py
